阅读 @qy 检查旧代码是否完成迁移, 如果没有完成, 设计计划并完成迁移工作(使用 3~5 个subagent).
上述全部完成后, run `make lint`, 检查是否存在警告和错误, 分析错误和警告的原因, 修复这些错误和警告(使用 3~5 个subagent,
禁止 ignore).
上述全部完成后, 检查 @todo.md 中的所有任务是否完成, 如果没有完成, 设计计划并完成这些任务(使用 3~5 个subagent).
上述全部完成后, 检查 @qy/ir 是否完成, 如果没有完成, 设计计划并完成IR设计工作(使用 3~5 个subagent).
上述全部完成后, 检查 @qy/backend/vm 是否完成, 如果没有完成, 设计计划并完成VM设计工作(使用 3~5 个subagent).
上述全部完成后, 检查 @qy/vm 是否完成, 如果没有完成, 设计计划并完成VM实现工作(使用 3~5 个subagent).

上述所有包含代码与测试, 每次执行完成后检查 模块存在的问题和改进点, 记录在 @todo.md 中.